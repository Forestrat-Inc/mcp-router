apiVersion: v1
kind: Service
metadata:
  name: mcp-router
  namespace: mcp
  labels:
    app: mcp-router
    app.kubernetes.io/component: mcp-router
    app.kubernetes.io/part-of: mcp-router
spec:
  type: ClusterIP
  selector:
    app: mcp-router
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP

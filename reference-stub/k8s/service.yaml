apiVersion: v1
kind: Service
metadata:
  name: mcp-stub
  namespace: aims
  labels:
    app: mcp-stub
    app.kubernetes.io/component: mcp-stub
    app.kubernetes.io/part-of: aims
spec:
  type: ClusterIP
  selector:
    app: mcp-stub
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP

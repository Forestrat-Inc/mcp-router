apiVersion: v1
kind: Service
metadata:
  name: mcp-stub
  namespace: mcp
  labels:
    app: mcp-stub
    app.kubernetes.io/component: mcp-stub
    app.kubernetes.io/part-of: mcp-router
spec:
  type: ClusterIP
  selector:
    app: mcp-stub
  ports:
    - name: http
      port: 9000
      targetPort: 9000
      protocol: TCP
